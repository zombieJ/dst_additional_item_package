# Agent Instructions

这些规则适用于本仓库内的智能体协作、代码修改、资源处理和 PR 描述。开始处理任务前先阅读本文件，并在没有用户明确覆盖要求时遵守这些约定。

## Lua 注释

- 新增或维护 Lua 方法/函数时，默认在方法上方写一行简短中文注释，说明职责或触发时机。
- 修改 hook、组件劫持、事件监听、队列、延迟逻辑时，保留必要中文注释。
- Lua 注释参考项目现有写法，默认使用 `--` 单行注释。
- 不使用 `--[[ ... ]]` 长注释，除非原文件已经采用这种风格。
- 注释必须单独成行，不要在同一行的 `--` 后继续写 Lua 代码。
- 简化或重构代码时，不要删掉解释为什么这样做的注释。

## PR 描述风格

- PR 标题默认使用 Conventional Commit + 中文功能摘要，例如 `feat: 支持农作物品质系统`。
- PR 正文参考 #132 的发布说明风格：使用一整段中文描述，不写 `Summary` / `Test` 模板小节。
- 正文从主题更新开头，例如“农作物品质系统更新，...”，随后用中文逗号和分号串联玩家可感知的主要内容、行为变化和修复点。
- 描述重点面向玩家和维护者可读的行为变化，少写实现细节；只有直接影响玩家使用的技术点才点到为止。
- PR 正文不要写开发相关内容，包括 `dev_mode`、调试工具、测试辅助、本地 Codex/技能/记忆文件、内部脚本和实现过程，除非用户明确要求。
- PR 正文不要写玩家无感知的仓库整理内容，包括资源目录调整、导出文件移动、旧压缩包清理、格式化、内部文档或配置整理。
- PR 正文避免写偏内部规则的细节，例如合堆限制、拆分继承、具体掉落继承链路；这类内容改写成玩家体验层面的概括。
- 如果变更很多，按“新增内容 -> 行为调整 -> 玩家可见修复”的顺序组织在同一段里。

## Git 分支

- `master` 是稳定主分支，用于承载已经合并完成、可作为当前正式代码基线的内容；同步仓库最新稳定代码时切换到 `master` 并从 `origin/master` 拉取。
- `next` 是长期集成分支，用于承接下一轮发布或合并前验证的 PR；创建或更新 PR 时，如果用户没有另行指定，优先确认目标是否应为 `next`，不要默认改到 `master`。
- 清理本地分支时，`master` 和 `next` 都是保护分支，不能当作普通已合并分支删除；即使 `next` 暂时显示 upstream gone 或已经合入 `master`，也不要主动清理。

## 版本号

- DST 模组版本号只维护在 `modinfo.lua` 顶部的 `version = "x.y.z"`；`package.json` / `package-lock.json` 里的 `version` 属于本地工具链，不作为模组发布版本。
- 只有在用户明确要求发版、更新版本号、准备发布或整理发布提交时才修改 `modinfo.lua` 的版本号；普通功能开发、修复、测试计划、PR 描述更新都不要顺手改版本。
- 修改版本时遵循三段式版本号：大版本用于不兼容或大范围系统变化，小版本用于玩家可感知的新内容，补丁版本用于 bug 修复、数值调整、资源补齐和小改动；没有特别说明时优先只递增补丁版本。
- 版本提交应尽量独立，提交信息使用 `chore: bump version to x.y.z` 或对应中文摘要，不和大批功能代码混在同一个提交里。

## DST Logs

- Local log directory: `[用户目录]\Documents\Klei\DoNotStarveTogether`

## 资源处理

- `exported`、`images` 等资源目录里的文件处理完成后，默认不要移动到 `exported_done`、`images_done` 或类似 `xxx_done` 目录中；除非用户明确要求移动。
- `exported_done` 是人工确认后的归档目录；新增或更新素材时，只把源导出文件放在 `exported`，不要主动写入或提交 `exported_done`。
- `gen/imageCutter/imageCutter.js` 可用于预处理 `_素材` 图片、透明背景、批量裁剪并导出到 `_素材/out`；不要使用这个脚本处理 `exported` 里的图片。
- 物品栏缩略图生成完成后，把 PNG 源图放在 `images/inventoryimages` 目录下，交给游戏启动时自动编译成可用的游戏文件。
- 不要主动运行 DST 编译工具生成 `anim/*.zip`、`*.tex`、`*.xml` 等编译产物；用户启动游戏时会自动编译。
- 除非用户明确要求，不要擅自将缩略图或其他图片放入 `images_done`。
- 如果误跑了编译工具，提交前清理本次新增的编译产物，只保留源码、`exported` 资源和必要的 PNG 源图，没有误跑则不用管。

## SCML 跟随贴图规则

- `Follower:FollowSymbol(parent.GUID, symbol, ...)` 跟的是实体级 `Follower`，目标 `symbol` 必须在 scml 里真实存在；推荐让 `folder`、PNG 路径、`object_ref name`、`timeline name` 使用同名锚点。
- 跟随锚点通常用 10x10 透明 PNG，只提供位置、角度、缩放、层级，不承担可见贴图。
- 所有需要支持跟随的动画都要在 mainline 和 timeline 里引用该锚点；当前动画缺少此 symbol 时，Follower 没有可靠挂载位置。
- Follower 跟随当前帧 symbol 的变换；锚点的 `x/y/angle/scale/pivot` 会影响子实体，锚点做动画时子实体也会随之移动。

## SCML 鼠标命中区域

- 不要用负数 `scale_x` 修正建筑、物品或皮肤贴图的左右方向；DST 的鼠标悬停、点击和清洁扫把换皮命中区域可能仍按未镜像的动画包围盒计算，导致部分可见贴图无法响应鼠标。
- 需要水平修正贴图方向时，优先翻转源 PNG 分片，保持 SCML timeline 里的 `scale_x` 为正数；同时把对应 `file pivot_x` 和 `object_ref abs_pivot_x` 改成 `1 - 原 pivot_x`，让实体锚点和视觉位置保持一致。
- 只在图片高度、上下裁切或视觉锚点实际变化时重算 `pivot_y`；单纯左右翻转不要顺手改 `pivot_y`。
