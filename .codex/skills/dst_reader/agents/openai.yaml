interface:
  display_name: "DST Reader"
  short_description: "Verify DST Lua APIs and inspect referenced animation resources."
  default_prompt: "Use $dst_reader to inspect the relevant DST base-game API or animation resource before modifying this mod."

policy:
  allow_implicit_invocation: true
